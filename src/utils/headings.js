const MyPlugin={
  install(Vue,options){
    //heading组件引进
    // Vue.component(......)
  }
}

// if (typeof window!=='undefined'&&window.Vue){
//   //使用插件使用Vue.use()
//   window.Vue.use(MyPlugin)
// }
